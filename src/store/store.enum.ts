export enum ReducerType {
	user = 'user',
	auth = 'auth',
	data = 'data',
}

export enum ActionType {
	authentication = 'authentication',
}

export enum ThunkType {
	login = 'login',
	logout = 'logout',
}
