export enum AxiosAPI {
	Login = 'login',
	Logout = 'logout',
}

export enum ReduxAPI {}

export enum HttpCode {
	BAD_REQUEST = 400,
	UNAUTHORIZED = 401,
	NOT_FOUND = 404,
}

export enum AuthenticationStatus {
	AUTH = 'AUTH',
	NO_AUTH = 'NO_AUTH',
	UNKNOWN = 'UNKNOWN',
}
